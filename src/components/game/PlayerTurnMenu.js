import React, {useState} from 'react'

const PlayerTurnMenu = ({}) => {
  return (
    <div>
      <button>check</button>
      <button>call</button>
      <button>raise</button>
    </div>
  )

}

export default PlayerTurnMenu